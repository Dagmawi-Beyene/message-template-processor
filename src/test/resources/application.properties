# AWS Configuration
aws.region=eu-west-1
aws.endpoint=http://localhost:4566
aws.accessKeyId=test
aws.secretKey=test

# S3 Configuration
aws.s3.bucket=test-bucket

# SQS Configuration
aws.sqs.queue-url=http://localhost:4566/000000000000/test-queue

# DynamoDB Configuration
aws.dynamodb.table=test-table

# Test logging
logging.level.org.springframework=DEBUG
logging.level.com.template=DEBUG

# AWS Configuration for tests
aws.region=us-east-1
aws.accessKeyId=test
aws.secretKey=test

# Disable cloud formation
spring.cloud.aws.cloudformation.enabled=false

# Disable stack configuration
spring.cloud.aws.stack.auto=false

# Disable region detection
spring.cloud.aws.region.auto=false
spring.cloud.aws.region.static=us-east-1

# Disable credentials detection
spring.cloud.aws.credentials.access-key=test
spring.cloud.aws.credentials.secret-key=test

# Disable AWS auto-configuration
spring.cloud.aws.region.static=us-east-1 